name: todo_dapp_front
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http:
  web3dart: 2.3.5
  # https://github.com/xclud/web3dart/issues/50
  web_socket_channel:
  provider:
  freezed:
  flutter_riverpod:
  cupertino_icons: ^1.0.2
  freezed_annotation: ^2.2.0
  flutter_slidable:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner:

flutter:
  uses-material-design: true
  assets:
    - lib/smartcontract/todo_contract.abi.json
